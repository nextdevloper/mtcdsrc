/**
 *
 */
package org.x.material.common.util;

import static org.junit.Assert.*;

import org.junit.Ignore;
import org.junit.runner.RunWith;
import org.seasar.framework.unit.Seasar2;

/**
 * @author reve
 *
 */
@RunWith(Seasar2.class)
public class CommonUtilTest {

	/**
	 * {@link org.x.material.common.util.CommonUtil#isError(java.lang.String)} のためのテスト・メソッド。
	 */
	@Ignore
	public void testIsError() {
		fail("まだ実装されていません");
	}

	/**
	 * {@link org.x.material.common.util.CommonUtil#prefixError(java.lang.String)} のためのテスト・メソッド。
	 */
	@Ignore
	public void testPrefixError() {
		fail("まだ実装されていません");
	}

	/**
	 * {@link org.x.material.common.util.CommonUtil#isENString(java.lang.String)} のためのテスト・メソッド。
	 */
	public void testIsENString() {

		String testString;

		testString = null;
		assertTrue(CommonUtil.isENString(testString));

		testString = "";
		assertTrue(CommonUtil.isENString(testString));

		testString = "123";
		assertFalse(CommonUtil.isENString(testString));

		testString = "   ";
		assertFalse(CommonUtil.isENString(testString));

//		fail("まだ実装されていません");
	}

}
