package org.x.material.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * アカウントマスタ（ブロック）リスナー.
 *
 * @author  nakada
 * @since   XXXXXXXX.01.00
 * @version 20141029.00.01
 */
public class MAccountBlockListener implements EntityListener<MAccountBlock> {

    @Override
    public void preInsert(final MAccountBlock entity, final PreInsertContext<MAccountBlock> context) {
    }

    @Override
    public void preUpdate(final MAccountBlock entity, final PreUpdateContext<MAccountBlock> context) {
    }

    @Override
    public void preDelete(final MAccountBlock entity, final PreDeleteContext<MAccountBlock> context) {
    }

    @Override
    public void postInsert(final MAccountBlock entity, final PostInsertContext<MAccountBlock> context) {
    }

    @Override
    public void postUpdate(final MAccountBlock entity, final PostUpdateContext<MAccountBlock> context) {
    }

    @Override
    public void postDelete(final MAccountBlock entity, final PostDeleteContext<MAccountBlock> context) {
    }
}