package org.x.material.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * メッセージ送信履歴（結果報告）リスナー.
 *
 * @author  nakada
 * @since   XXXXXXXX.01.00
 * @version 20141029.00.01
 */
public class TMsgSendResultListener implements EntityListener<TMsgSendResult> {

    @Override
    public void preInsert(final TMsgSendResult entity, final PreInsertContext<TMsgSendResult> context) {
    }

    @Override
    public void preUpdate(final TMsgSendResult entity, final PreUpdateContext<TMsgSendResult> context) {
    }

    @Override
    public void preDelete(final TMsgSendResult entity, final PreDeleteContext<TMsgSendResult> context) {
    }

    @Override
    public void postInsert(final TMsgSendResult entity, final PostInsertContext<TMsgSendResult> context) {
    }

    @Override
    public void postUpdate(final TMsgSendResult entity, final PostUpdateContext<TMsgSendResult> context) {
    }

    @Override
    public void postDelete(final TMsgSendResult entity, final PostDeleteContext<TMsgSendResult> context) {
    }
}