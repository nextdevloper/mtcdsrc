package org.x.material.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * マテリアルマスタリスナー.
 *
 * @author  nakada
 * @since   XXXXXXXX.01.00
 * @version 20141029.00.01
 */
public class MMaterialListener implements EntityListener<MMaterial> {

    @Override
    public void preInsert(final MMaterial entity, final PreInsertContext<MMaterial> context) {
    }

    @Override
    public void preUpdate(final MMaterial entity, final PreUpdateContext<MMaterial> context) {
    }

    @Override
    public void preDelete(final MMaterial entity, final PreDeleteContext<MMaterial> context) {
    }

    @Override
    public void postInsert(final MMaterial entity, final PostInsertContext<MMaterial> context) {
    }

    @Override
    public void postUpdate(final MMaterial entity, final PostUpdateContext<MMaterial> context) {
    }

    @Override
    public void postDelete(final MMaterial entity, final PostDeleteContext<MMaterial> context) {
    }
}