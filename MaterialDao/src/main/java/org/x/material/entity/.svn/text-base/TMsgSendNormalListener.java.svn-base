package org.x.material.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * メッセージ送信履歴（通常メッセージ）リスナー.
 *
 * @author  nakada
 * @since   XXXXXXXX.01.00
 * @version 20141029.00.01
 */
public class TMsgSendNormalListener implements EntityListener<TMsgSendNormal> {

    @Override
    public void preInsert(final TMsgSendNormal entity, final PreInsertContext<TMsgSendNormal> context) {
    }

    @Override
    public void preUpdate(final TMsgSendNormal entity, final PreUpdateContext<TMsgSendNormal> context) {
    }

    @Override
    public void preDelete(final TMsgSendNormal entity, final PreDeleteContext<TMsgSendNormal> context) {
    }

    @Override
    public void postInsert(final TMsgSendNormal entity, final PostInsertContext<TMsgSendNormal> context) {
    }

    @Override
    public void postUpdate(final TMsgSendNormal entity, final PostUpdateContext<TMsgSendNormal> context) {
    }

    @Override
    public void postDelete(final TMsgSendNormal entity, final PostDeleteContext<TMsgSendNormal> context) {
    }
}