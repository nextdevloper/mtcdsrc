package org.x.material.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * コードマスタリスナー.
 *
 * @author  nakada
 * @since   XXXXXXXX.01.00
 * @version 20141029.00.01
 */
public class MCodeListener implements EntityListener<MCode> {

    @Override
    public void preInsert(final MCode entity, final PreInsertContext<MCode> context) {
    }

    @Override
    public void preUpdate(final MCode entity, final PreUpdateContext<MCode> context) {
    }

    @Override
    public void preDelete(final MCode entity, final PreDeleteContext<MCode> context) {
    }

    @Override
    public void postInsert(final MCode entity, final PostInsertContext<MCode> context) {
    }

    @Override
    public void postUpdate(final MCode entity, final PostUpdateContext<MCode> context) {
    }

    @Override
    public void postDelete(final MCode entity, final PostDeleteContext<MCode> context) {
    }
}